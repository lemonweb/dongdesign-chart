# 30d GMV Conversion VibeCode

本项目用于验证「最近 30 天成交金额与转化率趋势」在 VibeCode 路径下的图表生成质量。

它是可运行原型，不属于 Wiki 规范文档。

## 打开方式

直接用浏览器打开：

```txt
index.html
```

## 验证范围

- ECharts / G2 / SVG 三种实现切换。
- 折线图 legend shape。
- 笛卡尔坐标轴、分隔线、刻度数量。
- Tooltip 与 hover 指示线。
- G2 自定义 hover layer 与 MarkPoint 对齐。
