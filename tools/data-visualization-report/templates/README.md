# templates · 页面级模板 profile

模板负责**页面骨架**：模块顺序、外层结构、默认开关。组件样式来自 `modules/`，视觉/布局来自 `rules/`。新增报告类型时在此加一份 profile，不改组件与全局规则。

- `data-interpretation-report.md` —— 数据解读报告通用模板（单品/大盘/归因等均适用）。

新增模板：复制一份 profile，调整模块顺序与默认开关即可（如财务报告可默认开启瀑布图、市场调研默认占比环）。
